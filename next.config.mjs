/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // basePath: "/Elderbrew",
  assetPrefix: '/',
  compiler: {
    styledComponents: true,
  },
};

export default nextConfig;
